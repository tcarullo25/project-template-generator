# Copy to .env.local for frontend-only overrides. Vite exposes variables
# prefixed with VITE_ to the browser bundle; never put a secret in one.

# Base URL for API calls. Leave empty to use the current origin, which lets the
# Vite dev server proxy /api to the backend (see vite.config.ts).
VITE_API_BASE_URL=
